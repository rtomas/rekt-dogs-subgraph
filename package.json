{
  "name": "rekt-dogs",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ rtomas/rekt-dogs",
    "create-local": "graph create --node http://localhost:8020/ rtomas/rekt-dogs",
    "remove-local": "graph remove --node http://localhost:8020/ rtomas/rekt-dogs",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 rtomas/rekt-dogs",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.54.0",
    "@graphprotocol/graph-ts": "0.30.0",
    "@protofire/subgraph-toolkit": "^0.1.2"
  },
  "devDependencies": {
    "matchstick-as": "0.5.0"
  }
}
